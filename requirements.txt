# Chitralahar CMS — dependencies
Flask==3.1.3
Pillow==11.3.0
Markdown==3.9
bleach>=6.1
python-slugify>=8.0
# Production WSGI server (used by deploy/chitralahar.service + the Apache
# reverse-proxy setup). Not needed for local `python run.py`.
gunicorn>=21,<24
# Optional: HEIC/HEIF support (iPhone photos). Uncomment if you want it and it
# builds on your machine — the app runs fine without it (HEIC uploads are skipped).
# pillow-heif>=0.16
